# Python Console Apps

A repository for different console apps using Python.

#### Instructions to run banking_application.py:

- cd into the banking-application folder
- Run the command `pip install -r requirements.txt`
- Then run `python banking_application.py`
